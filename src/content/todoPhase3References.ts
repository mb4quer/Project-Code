import type { Files } from '../contracts';

export const todoHtml = `<!doctype html>
<html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>Small steps Todo</title><link rel="stylesheet" href="./styles.css"></head>
<body><main class="todo-app"><h1>Small steps</h1><section aria-labelledby="file-title"><h2 id="file-title">File plan</h2><ul id="file-plan"><li>index.html: semantic structure</li><li>styles.css: visual rules</li><li>model.js: task data operations</li><li>main.js: DOM events and rendering</li></ul></section><section aria-labelledby="flow-title"><h2 id="flow-title">Flow plan</h2><p id="flow-summary">Add, complete, and delete tasks; check empty, invalid-input, and filtered results.</p><ul id="flow-list"><li>Add trimmed task text</li><li>Complete one task by id</li><li>Delete one task by id</li></ul></section><form id="todo-form"><label for="task-input">New task</label><input id="task-input" autocomplete="off"><button type="submit">Add task</button></form><p id="input-error" role="alert"></p><div id="filter-controls" aria-label="Task filters"><button type="button" data-filter="all">All</button><button type="button" data-filter="active">Active</button><button type="button" data-filter="completed">Completed</button></div><p id="task-count" aria-live="polite">0 of 0 complete</p><p id="empty-state" aria-live="polite"></p><ul id="task-list" aria-live="polite"></ul></main><script type="module" src="./main.js"></script></body></html>`;

export const todoCss = `:root { font-family: system-ui, sans-serif; color: #172033; background: #f5f7fb; } body { margin: 0; } .todo-app { max-width: 38rem; margin: 2rem auto; padding: 1.25rem; background: white; overflow-wrap:anywhere; } form, #filter-controls { display:flex; gap:.5rem; flex-wrap:wrap; } input { min-width:0; width:min(100%, 18rem); padding:.5rem; } button { padding:.5rem .8rem; } #task-list { list-style:none; padding:0; } #task-list li { display:flex; gap:.5rem; padding:.45rem 0; min-width:0; } .task-label { flex:1; min-width:0; overflow-wrap:anywhere; } .done { text-decoration:line-through; color:#667085; } #input-error { color:#b42318; min-height:1.2em; }`;

export const baseModel = `let nextId = 1;
export function addTask(tasks, rawText) { const text = rawText.trim(); if (!text) return tasks; return [...tasks, { id: nextId++, text, done: false }]; }
export function toggleTask(tasks, id) { return tasks.map(task => task.id === id ? { ...task, done: !task.done } : task); }
export function deleteTask(tasks, id) { return tasks.filter(task => task.id !== id); }
export function filteredTasks(tasks, filter) { return filter === 'active' ? tasks.filter(task => !task.done) : filter === 'completed' ? tasks.filter(task => task.done) : tasks; }`;

export const fullMain = `import { addTask, deleteTask, filteredTasks, toggleTask } from './model.js';
const form = document.querySelector('#todo-form'); const input = document.querySelector('#task-input'); const list = document.querySelector('#task-list'); const count = document.querySelector('#task-count'); const error = document.querySelector('#input-error'); const empty = document.querySelector('#empty-state');
let tasks = []; let filter = 'all';
function render() { const shown = filteredTasks(tasks, filter); list.replaceChildren(...shown.map(task => { const row = document.createElement('li'); const box = document.createElement('input'); box.type = 'checkbox'; box.checked = task.done; box.setAttribute('aria-label', 'Complete ' + task.text); box.addEventListener('change', () => { tasks = toggleTask(tasks, task.id); render(); }); const label = document.createElement('span'); label.className = 'task-label' + (task.done ? ' done' : ''); label.textContent = task.text; const remove = document.createElement('button'); remove.type = 'button'; remove.className = 'delete-task'; remove.textContent = 'Delete'; remove.addEventListener('click', () => { tasks = deleteTask(tasks, task.id); render(); }); row.append(box, label, remove); return row; })); count.textContent = tasks.filter(task => task.done).length + ' of ' + tasks.length + ' complete'; empty.textContent = tasks.length === 0 ? 'No tasks yet. Add one above.' : shown.length === 0 ? 'No tasks match this filter.' : ''; }
form.addEventListener('submit', event => { event.preventDefault(); const next = addTask(tasks, input.value); error.textContent = next === tasks ? 'Enter a task before adding it.' : ''; if (next !== tasks) input.value = ''; tasks = next; render(); });
document.querySelectorAll('[data-filter]').forEach(button => button.addEventListener('click', () => { filter = button.dataset.filter; render(); })); render();`;

export const fullFiles: Files = { 'index.html': todoHtml, 'styles.css': todoCss, 'model.js': baseModel, 'main.js': fullMain };
export const coreHtml = todoHtml.replace(/<section aria-labelledby="(?:flow|file)-title">.*?<\/section>/g, '').replace('<p id="input-error" role="alert"></p>', '').replace('<div id="filter-controls" aria-label="Task filters"><button type="button" data-filter="all">All</button><button type="button" data-filter="active">Active</button><button type="button" data-filter="completed">Completed</button></div>', '').replace('<p id="empty-state" aria-live="polite"></p>', '');
export const coreMain = `import { addTask, deleteTask, toggleTask } from './model.js';
const form = document.querySelector('#todo-form'); const input = document.querySelector('#task-input'); const list = document.querySelector('#task-list'); const count = document.querySelector('#task-count'); let tasks = [];
function render() { list.replaceChildren(...tasks.map(task => { const row = document.createElement('li'); const box = document.createElement('input'); box.type = 'checkbox'; box.checked = task.done; box.setAttribute('aria-label', 'Complete ' + task.text); box.addEventListener('change', () => { tasks = toggleTask(tasks, task.id); render(); }); const label = document.createElement('span'); label.className = 'task-label' + (task.done ? ' done' : ''); label.textContent = task.text; const remove = document.createElement('button'); remove.type = 'button'; remove.className = 'delete-task'; remove.textContent = 'Delete'; remove.addEventListener('click', () => { tasks = deleteTask(tasks, task.id); render(); }); row.append(box, label, remove); return row; })); count.textContent = tasks.filter(task => task.done).length + ' of ' + tasks.length + ' complete'; }
form.addEventListener('submit', event => { event.preventDefault(); const next = addTask(tasks, input.value); if (next !== tasks) input.value = ''; tasks = next; render(); }); render();`;
export const coreFiles: Files = { 'index.html': coreHtml, 'styles.css': todoCss, 'model.js': baseModel, 'main.js': coreMain };

export const filterStarter: Files = { ...fullFiles, 'main.js': fullMain.replace("const shown = filteredTasks(tasks, filter);", "const shown = tasks;").replace("error.textContent = next === tasks ? 'Enter a task before adding it.' : '';", "error.textContent = ''; ").replace("document.querySelectorAll('[data-filter]').forEach(button => button.addEventListener('click', () => { filter = button.dataset.filter; render(); }));", "") };

export const storageModel = `let nextId = 1;
export function validTasks(value) { return Array.isArray(value) && value.every(task => task && Number.isSafeInteger(task.id) && task.id > 0 && task.id < Number.MAX_SAFE_INTEGER && typeof task.text === 'string' && task.text.trim().length > 0 && task.text.length <= 240 && typeof task.done === 'boolean') && new Set(value.map(task => task.id)).size === value.length; }
export function restoreTasks(value) { if (!validTasks(value)) throw new Error('invalid or exhausted saved tasks'); nextId = Math.max(0, ...value.map(task => task.id)) + 1; return value.map(task => ({ ...task })); }
export function addTask(tasks, rawText) { const text = rawText.trim(); if (!text || text.length > 240) return tasks; if (nextId >= Number.MAX_SAFE_INTEGER) nextId = 1; while (tasks.some(task => task.id === nextId)) { nextId += 1; if (nextId >= Number.MAX_SAFE_INTEGER) nextId = 1; } const id = nextId++; return [...tasks, { id, text, done: false }]; }
export function toggleTask(tasks, id) { return tasks.map(task => task.id === id ? { ...task, done: !task.done } : task); }
export function deleteTask(tasks, id) { return tasks.filter(task => task.id !== id); }
export function filteredTasks(tasks, filter) { return filter === 'active' ? tasks.filter(task => !task.done) : filter === 'completed' ? tasks.filter(task => task.done) : tasks; }`;
export const storageMain = `import { addTask, deleteTask, filteredTasks, restoreTasks, toggleTask } from './model.js';
const form = document.querySelector('#todo-form'); const input = document.querySelector('#task-input'); const list = document.querySelector('#task-list'); const count = document.querySelector('#task-count'); const error = document.querySelector('#input-error'); const empty = document.querySelector('#empty-state'); const status = document.querySelector('#storage-status'); let tasks = []; let filter = 'all';
const storage = window.trainingStorage;
let writes = Promise.resolve(); let ready = false; input.disabled = true; form.querySelector('button').disabled = true;
function save() { const snapshot = JSON.stringify(tasks); writes = writes.catch(() => undefined).then(async () => { try { await storage.setItem('tasks', snapshot); status.textContent = 'Saved tasks.'; } catch { status.textContent = 'Could not save tasks. Your current list is still visible.'; } }); return writes; }
function render() { const shown = filteredTasks(tasks, filter); list.replaceChildren(...shown.map(task => { const row = document.createElement('li'); const box = document.createElement('input'); box.type = 'checkbox'; box.checked = task.done; box.setAttribute('aria-label', 'Complete ' + task.text); box.addEventListener('change', async () => { tasks = toggleTask(tasks, task.id); render(); await save(); }); const label = document.createElement('span'); label.className = 'task-label' + (task.done ? ' done' : ''); label.textContent = task.text; const remove = document.createElement('button'); remove.type = 'button'; remove.className = 'delete-task'; remove.textContent = 'Delete'; remove.addEventListener('click', async () => { tasks = deleteTask(tasks, task.id); render(); await save(); }); row.append(box, label, remove); return row; })); count.textContent = tasks.filter(task => task.done).length + ' of ' + tasks.length + ' complete'; empty.textContent = tasks.length === 0 ? 'No tasks yet. Add one above.' : shown.length === 0 ? 'No tasks match this filter.' : ''; }
form.addEventListener('submit', async event => { event.preventDefault(); if (!ready) return; const next = addTask(tasks, input.value); error.textContent = next === tasks ? (input.value.trim().length > 240 ? 'Tasks must be 240 characters or fewer.' : 'Enter a task before adding it.') : ''; if (next !== tasks) { input.value = ''; tasks = next; render(); await save(); } }); document.querySelectorAll('[data-filter]').forEach(button => button.addEventListener('click', () => { filter = button.dataset.filter; render(); }));
async function restore() { try { const raw = await storage.getItem('tasks'); tasks = raw === null ? [] : restoreTasks(JSON.parse(raw)); render(); status.textContent = raw === null ? 'No saved tasks yet.' : 'Restored saved tasks.'; } catch { tasks = []; render(); status.textContent = 'Saved tasks were unavailable; starting with an empty list.'; } finally { ready = true; input.disabled = false; form.querySelector('button').disabled = false; } } restore();`;
export const storageFiles: Files = { ...fullFiles, 'index.html': todoHtml.replace('<ul id="task-list" aria-live="polite"></ul>', '<ul id="task-list" aria-live="polite"></ul><p id="storage-status" aria-live="polite"></p>'), 'model.js': storageModel, 'main.js': storageMain };
export const storageStarter: Files = { ...fullFiles, 'main.js': fullMain };

export const planningFiles: Files = { ...coreFiles, 'index.html': coreHtml.replace('<p id="task-count"', '<section aria-labelledby="flow-title"><h2 id="flow-title">Flow plan</h2><p id="flow-summary">Add, complete, and delete tasks; check empty, invalid-input, and filtered results.</p><ul id="flow-list"><li>Add trimmed task text</li><li>Complete one task by id</li><li>Delete one task by id</li></ul></section><p id="task-count"') };
export const stackFiles: Files = { ...planningFiles, 'index.html': planningFiles['index.html'].replace('<section aria-labelledby="flow-title">', '<section aria-labelledby="file-title"><h2 id="file-title">File plan</h2><ul id="file-plan"><li>index.html: semantic structure</li><li>styles.css: visual rules</li><li>model.js: task data operations</li><li>main.js: DOM events and rendering</li></ul></section><section aria-labelledby="flow-title">') };
/** The setup shell is additive: preserve the completed CRUD handlers while making every later selector visible. */
export const layoutFiles: Files = { ...fullFiles, 'main.js': coreMain };
export const testExportFiles: Files = { ...storageFiles, 'main.js': `${storageMain}\nconst testButton = document.querySelector('#run-smoke-test'); const results = document.querySelector('#test-results'); const exportButton = document.querySelector('#prepare-export'); const exportStatus = document.querySelector('#export-status'); let smokeRunning = false; const interactive = () => Array.from(document.querySelectorAll('#todo-form input, #todo-form button, #filter-controls button, #task-list input, #task-list button')); function setSmokeLock(locked) { interactive().forEach(control => { control.disabled = locked; }); testButton.disabled = locked; } const originalRender = render; render = () => { originalRender(); if (smokeRunning) setSmokeLock(true); }; testButton.addEventListener('click', async () => { if (smokeRunning) return; smokeRunning = true; const disabled = interactive().map(control => [control, control.disabled]); const testWasDisabled = testButton.disabled; const inputValue = input.value; const errorValue = error.textContent; const statusValue = status.textContent; let priorTasks; let priorFilter; let priorSaved; let failure; try { if (!ready) throw new Error('storage recovery is still loading'); await writes; priorTasks = tasks.map(task => ({ ...task })); priorFilter = filter; priorSaved = await storage.getItem('tasks'); setSmokeLock(true); filter = 'all'; render(); const beforeRows = list.querySelectorAll('li').length; const beforeComplete = tasks.filter(task => task.done).length; input.value = '__smoke_task__'; const submitEvent = new Event('submit', { bubbles: true, cancelable: true }); form.dispatchEvent(submitEvent); await writes; const added = Array.from(list.querySelectorAll('li')).find(row => row.querySelector('.task-label')?.textContent === '__smoke_task__'); if (!submitEvent.defaultPrevented || list.querySelectorAll('li').length !== beforeRows + 1 || !added) throw new Error('add assertion failed'); const firstBox = added.querySelector('input[type="checkbox"]'); firstBox.checked = true; firstBox.dispatchEvent(new Event('change', { bubbles: true })); await writes; const visible = Array.from(list.querySelectorAll('li')).find(row => row.querySelector('.task-label')?.textContent === '__smoke_task__'); const visibleBox = visible?.querySelector('input[type="checkbox"]'); const saved = await storage.getItem('tasks'); if (!visibleBox?.checked || tasks.filter(task => task.done).length !== beforeComplete + 1 || !saved?.includes('__smoke_task__')) throw new Error('toggle or save assertion failed'); } catch (caught) { failure = caught instanceof Error ? caught : new Error(String(caught)); } finally { try { if (priorTasks) { tasks = priorTasks; filter = priorFilter; render(); if (priorSaved === null) await storage.removeItem('tasks'); else await storage.setItem('tasks', priorSaved); } } catch (caught) { failure = new Error('cleanup failed: ' + (caught instanceof Error ? caught.message : String(caught))); } smokeRunning = false; disabled.forEach(([control, wasDisabled]) => { control.disabled = wasDisabled; }); testButton.disabled = testWasDisabled; input.value = inputValue; error.textContent = errorValue; status.textContent = statusValue; results.textContent = failure ? 'Smoke test failed: ' + failure.message : 'Smoke test passed: add, toggle, saved state, and cleanup all produced the expected result.'; } }); exportButton.addEventListener('click', () => { exportStatus.textContent = 'Export ready: keep index.html, styles.css, model.js, and main.js together; serve the folder with a local static server for module loading.'; });` };
Object.assign(testExportFiles, { 'index.html': storageFiles['index.html'].replace('</main>', '<section id="test-panel"><button type="button" id="run-smoke-test">Run smoke test</button><p id="test-results" aria-live="polite"></p><button type="button" id="prepare-export">Prepare export</button><p id="export-status" aria-live="polite"></p></section></main>') });
Object.assign(testExportFiles, { 'main.js': testExportFiles['main.js'].replace("results.textContent = failure ? 'Smoke test failed: ' + failure.message : 'Smoke test passed: add, toggle, saved state, and cleanup all produced the expected result.';", "if (failure) results.textContent = 'Smoke test failed: ' + failure.message; else results.textContent = 'Smoke test passed: add, toggle, saved state, and cleanup all produced the expected result.';") });
Object.assign(testExportFiles, { 'main.js': testExportFiles['main.js'].replace("function setSmokeLock(locked) { interactive().forEach(control => { control.disabled = locked; }); testButton.disabled = locked; }", "function setSmokeLock(locked) { interactive().forEach(control => { control.disabled = locked; }); testButton.disabled = locked; }").replace("if (!ready) throw new Error('storage recovery is still loading'); await writes;", "if (!ready) throw new Error('storage recovery is still loading'); setSmokeLock(true); await writes;").replace('let priorTasks; let priorFilter; let priorSaved; let failure;', 'let priorTasks; let priorFilter; let priorSaved; let snapshotReady = false; let failure;').replace('priorSaved = await storage.getItem(\'tasks\'); setSmokeLock(true);', 'priorSaved = await storage.getItem(\'tasks\'); snapshotReady = true; setSmokeLock(true);').replace("!saved?.includes('__smoke_task__')", "!JSON.parse(saved || '[]').some(task => task.text === '__smoke_task__' && task.done === true)").replace('if (priorTasks) { tasks = priorTasks;', 'if (snapshotReady) { tasks = priorTasks;').replace('smokeRunning = false; disabled.forEach(([control, wasDisabled]) => { control.disabled = wasDisabled; }); testButton.disabled = testWasDisabled;', 'smokeRunning = false; setSmokeLock(false);') });
export const testExportStarter: Files = { ...fullFiles, 'main.js': fullMain };
