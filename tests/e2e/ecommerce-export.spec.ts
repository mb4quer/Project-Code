import { expect, test } from '@playwright/test';
import { readFile } from 'node:fs/promises';
import { testFiles } from '../../src/content/ecommerceReferences';
import { curriculum } from '../../src/content/curriculum';
import { createInitialSession } from '../../src/data/demos';
import { completeActivity, initializeProjectWorkspace, recordBlankAttempt, recordChallengeResult, setLearningLocation, submitQuestionAnswer } from '../../src/lesson/engine';
function unzip(bytes: Buffer): Record<string,string> { const out:Record<string,string>={};let p=0;while(bytes.readUInt32LE(p)===0x04034b50){const s=bytes.readUInt32LE(p+18),n=bytes.readUInt16LE(p+26),x=bytes.readUInt16LE(p+28),name=bytes.subarray(p+30,p+30+n).toString(),start=p+30+n+x;out[name]=bytes.subarray(start,start+s).toString();p=start+s}return out; }
test('Cedar & Thread export preserves source and runs standalone',async({page,context})=>{test.setTimeout(120000);let seed=initializeProjectWorkspace(createInitialSession(),'amazon-inspired-ecommerce',{...testFiles,'notes.txt':'Cedar export note'});for(const topic of curriculum.topics.filter(t=>t.projectId!=='amazon-inspired-ecommerce')){for(const a of topic.activities)seed=completeActivity(seed,topic.id,a.id);for(const b of topic.blanks)seed=recordBlankAttempt(seed,topic,b,b.acceptedAnswers[0]).session;for(const q of topic.assessmentQuestions.slice(0,10))seed=submitQuestionAnswer(seed,topic,q,q.acceptedAnswers[0]).session;for(const c of topic.challenges)seed=recordChallengeResult(seed,topic.id,c.id,true)}seed=setLearningLocation(seed,{view:'topic',projectId:'amazon-inspired-ecommerce',topicId:'shop-testing-export-and-handoff',stepId:'shop-testing-export-and-handoff-guided-2'});await page.addInitScript(s=>localStorage.setItem('project-code.session.v1',JSON.stringify(s)),seed);await page.goto('/');const pending=page.waitForEvent('download');await page.locator('#export-project').click();const download=await pending;await download.saveAs('test-results/ecommerce-store.zip');const files=unzip(await readFile((await download.path())!));for(const [name,source]of Object.entries(testFiles))expect(files['source/'+name]).toBe(source);expect(files['source/notes.txt']).toBe('Cedar export note');const app=await context.newPage();await app.route('http://ecommerce-export.test/**',r=>r.fulfill({contentType:'text/html',body:files['index.html']}));await app.goto('http://ecommerce-export.test/#/catalog');await app.locator('a[href="#/product/ember-mug"]').click();await expect(app.locator('#product-detail')).toBeVisible();await app.locator('[data-cart-add="ember-mug"]').click();await app.locator('a[href="#/cart"]').click();await expect(app.locator('#cart-list')).toContainText('Ember Mug');await app.reload();await expect(app.locator('#cart-list')).toContainText('Ember Mug');await app.goto('http://ecommerce-export.test/#/checkout');await app.locator('#checkout-delivery').selectOption('pickup-demo');await app.locator('#checkout-consent').check();await app.locator('#checkout-outcome').selectOption('decline');await app.locator('#checkout-submit').click();await expect(app.locator('#checkout-status')).toContainText('declined');await app.goto('http://ecommerce-export.test/#/catalog');await app.locator('[data-wishlist-toggle="ember-mug"]').click();await expect(app.locator('#wishlist-count')).toHaveText('1');await app.reload();await expect(app.locator('#wishlist-count')).toHaveText('0');for(let n=0;n<2;n++){await app.locator('#run-shop-smoke').click();await expect(app.locator('#shop-smoke-status')).toContainText('passed')}await app.setViewportSize({width:390,height:844});expect(await app.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBe(true);await app.screenshot({path:'test-results/ecommerce-export-mobile.png',fullPage:true});});
