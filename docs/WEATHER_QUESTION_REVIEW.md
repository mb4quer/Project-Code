# Async Weather question review

Astra accepted the final static banks on 2026-09-14 after reviewing Luna drafts against the implemented fixture API, reference programs, and progression engine. All 192 questions are marked reviewed: 24 per topic, six each of prediction, debugging, explanation, and application. Every equivalence group has at least two items; each topic has multiple equivalent questions per concept so reveal can retire an item and require fresh work. Question IDs are stable; no Phase 2/3 bank changed.

The final review corrected ambiguous mounted-element and module-path prompts; named import alias syntax; local Promise versus response-JSON prerequisites; valid concurrent-call answers; finite temperature and zero-Celsius handling; explicit null, HTTP 503, malformed, and rejected responses; and increment-before-validation request identities. Success, catch, mode changes, and optional finally cleanup use the same newest-request rule.

Testing/export questions now cover the compiled root index.html, exact source/ preservation, rebuilding by exporting again, static serving, deterministic defaults, explicit live opt-in, and smoke-test state restoration. Smoke must refuse pending user work and must assert actual output rather than print a pass. Choice options are stored in A/B display order with the matching accepted label, without a runtime label-flipping generator.

Review was editorial: automated validation confirms structure, IDs, category coverage, reviewed flags and progression invariants, but does not establish semantic correctness by itself. The browser journey answers ten distinct questions per topic, retires a revealed item, and resumes after five credits; unit coverage exercises exhaustion, misses, fresh equivalents, and preserved mastery.
