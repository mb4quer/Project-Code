# Phase 2 content review

Reviewed by Astra on 2026-09-12 after a Luna draft and an instructional revision. Published topic: `todo-dom-events-and-task-state`, in `src/content/domTodo.ts`; reviewed bank: `src/content/domQuestions.ts`.

Five activities alternate reading/guided/reading/guided/reading. Review added concrete plain-files/Vite/React comparisons, import/export boundaries, array operations, callbacks, stable counters and live checkbox properties before assessments rely on them. Corrected a render example to return li rows, removed the unassessed clear-completed extension, and made the add-only count accurately reflect the total. Guided step two extends the saved step-one workspace.

The bank has 32 stable distinct IDs and identities, eight questions in each of prediction, debugging, explanation and application. Astra replaced broad free-text explanation questions with labeled choices, narrowed code questions to explicit blanks or exact output, preserved internal whitespace for exact-label/code answers, and enumerated valid quote alternatives. Questions marked reviewed were read against the teaching text and reference programs; this is editorial review, not an assertion made by the schema validator.

The final eight reserve scenarios use materially different evidence: ID collision after deletion; a DOM-only checkbox change erased by rendering; deletion of duplicate labels; checked versus value; a delete button inside a form; the nonmatching map branch; an invalid-input early return; and an add/toggle/delete/add sequence. They are concept-equivalent work within events, rendering, identity and state groups. They are not renamed copies or merely reordered answers. The scheduler requires an unseen same-group alternative before permitting reveal, then permanently retires the revealed identity.

Four blanks cover conceptual and code reasoning, with declared normalization. Exactly three challenges apply, debug and independently combine the same useful Todo contract. Reference maps are runnable HTML/CSS/JS/modules; each challenge remains isolated from the project draft. Read-only references support comparison without replacing learner code.

Behavioral review is backed by the browser matrix: all two guided and three challenge references pass; every starter fails its assigned assessment. Incorrect programs cover missing trim, accepting whitespace, parsing labels as HTML, uncleared input, toggling all rows, deleting duplicates together, stale counts, and duplicate submit handlers. Syntax errors and a guarded infinite loop cannot receive a pass. Full UI checks cover end-to-end completion, halfway reload, reveals, exhaustion, separate drafts and successor unlocking. Full acceptance evidence and limitations are in PHASES.md and HANDOFF.md.

## Phase 3 editorial and implementation review

Astra reviewed the six new banks drafted by Luna, then made the publication change to `reviewed: true`. There are 144 new stable IDs/identities, 24 per topic and six per reasoning category. Questions use narrow code blanks or explicit choices; conceptual normalization remains separate from case-sensitive code normalization. Review corrected a nested-module path, the false claim that JSON.parse(null) throws, invalid prose in a code answer, unspecified label/live-region context, duplicated module-path questions, and inaccurate source-versus-bundle export expectations. New application work includes named-import aliases, ordered write snapshots, deferred module scripts and duplicate HTML-id repair. Matching prerequisites were added to the readings. A final Luna read-only pass led to consistent canonical test selectors, observable invalid-input assertions and clearer basic/full metadata.

The initial Terra curriculum draft was not accepted unchanged. Astra replaced generic reading/instruction text with concrete incremental lesson steps, separated basic and complete objectives, added topic-specific blanks, and made apply/debug/combine drafts distinct. Examples preserve the established CRUD project, add visible planning/file sections, teach Promise/async/await and JSON before storage use, and explain source autosave versus preview task data. The first smoke example merely printed success; review required actual DOM/storage assertions, negative behavior detection and cleanup of prior user state. Storage review required restored-id initialization, schema validation, a startup edit guard and ordered snapshot writes.

Grading remains deterministic and educational. Planning text checks verify declared terms and per-file responsibilities, not open-ended semantic correctness; complete planning/file/layout exercises also exercise the working app. Browser matrices run references and unfinished/incorrect programs. Storage fixtures run in independent frames and never use a learner's saved training data. Full acceptance results, regressions found and limitations are recorded in PHASES.md and HANDOFF.md.
